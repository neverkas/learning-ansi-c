#include <stdio.h>

int main(){
  // int c;

  // while(c = getchar() != EOF){ // loops |word|+1 times. (+1) is due to the '\0' character
  //   printf("%d\n", c);
  // }

  printf("%d\n", EOF);

  return 0;
}